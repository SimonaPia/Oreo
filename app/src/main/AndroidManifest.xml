<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PronuntiApp_Oreo_sms2324"
        tools:targetApi="27"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".Welcome"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PronuntiApp_Oreo_sms2324">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ExerciseListActivity"
            android:exported="true">
        </activity>

        <activity android:name=".NewAppointmentActivity"
            android:exported="true">
        </activity>

        <activity android:name=".HomePageLogopedista"></activity>
        <activity android:name=".HomePage"></activity>
        <activity android:name=".IntermediatePage"></activity>
        <activity android:name=".HomePageGenitore"></activity>
    </application>

</manifest>
