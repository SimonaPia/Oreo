<!-- item_exercise.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/exercise_name_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Spaziatura tra il nome dell'esercizio e i bottoni -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="8dp" />

    <!-- Layout orizzontale per i bottoni -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <Button
            android:id="@+id/details_button_id"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Dettagli"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="8dp"
            android:backgroundTint="@color/blu_oltremare"
            android:clickable="true"
            android:textColor="@android:color/white"/>

        <Button
            android:id="@+id/assign_button_id"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Assegna"
            android:layout_marginTop="8dp"
            android:layout_marginStart="8dp"
            android:layout_marginBottom="8dp"
            android:backgroundTint="@color/blu_oltremare"
            android:clickable="true"
            android:textColor="@android:color/white"/>
    </LinearLayout>

    <!-- Spaziatura sotto i bottoni -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="8dp" />

    <!-- Separatore -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@android:color/darker_gray" />
</LinearLayout>
